<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:statusBarColor">@color/colorBrown</item>
    </style>

    <style name="AppTheme.Splash" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@color/colorYellow</item>
    </style>

    <style name="AppTheme.Toolbar" parent="AppTheme">
        <item name="android:titleTextColor">@color/colorYellow</item>
        <item name="android:subtitleTextColor">@color/colorYellow</item>
        <item name="android:textColorSecondary">@color/colorYellow</item>
        <item name="android:textColorPrimary">@color/colorYellow</item>
    </style>

</resources>
